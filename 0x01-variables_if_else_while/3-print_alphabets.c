#include <stdio.h>
/**
  * main - entry point
  *Return: always 0 (success)
  */

int main(void)
{
	char b;

	char c;

	b = 'a';
	c = 'A';
	while
		(b <= 'a') {
			putchar(b);
			b++;
		}
	while 
		(c <= 'A') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}

